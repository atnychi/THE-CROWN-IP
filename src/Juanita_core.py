# Core recursive encryption engine

class JuanitaCore:
    def encrypt(self, data, key):
        # Placeholder for recursive encryption (simple reverse)
        return data[::-1]

    def decrypt(self, data, key):
        # Placeholder for recursive decryption (simple reverse)
        return data[::-1]
